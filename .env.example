# Copy to .env and fill in. .env is gitignored; these settings can also be
# passed as --key / --model / --prompt, or set in the environment.
OPENROUTER_API_KEY=sk-or-v1-...
OPENROUTER_MODEL=anthropic/claude-3.5-sonnet
# POKEMCP_PROMPT=Head north out of town and catch something.
